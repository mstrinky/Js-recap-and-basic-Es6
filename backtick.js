// string a multiline lekhar jonnoo backtick use kora hoi
const multiline = `amar sonar bangla 
ami tomai valobasi
chirodin tomar akas
tomar batas`;
console.log(multiline);

// backtick use korar r akta subidha holo ${ } ata.ata bosale kono akta variable er man sorasori bosabe.
// `${}` ata use korle barbar + use korar dorkar hoina.
const a = 21;
const b = 12;
const newSummery = `sum of ${a} and ${b} is:${a+b}`;
console.log(newSummery);
